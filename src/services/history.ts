export class HistoryService {
    static getHistory(server: string, item: string, city: string) {
        if (server === "west") { 

        }
        if (server === "east") {

        }
        
    }

    private fetchHistory(server: string, item: string, city: string) {

    }
}